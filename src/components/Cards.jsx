import React from 'react';

const Cards = () => {
  return (
    <div>
      <h1>
        This'll be where you can iterate through the data and put them in cards
      </h1>
    </div>
  );
};

export default Cards;
